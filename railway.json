{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm build --filter=@alpha/api"
  },
  "deploy": {
    "startCommand": "pnpm --filter @alpha/api start:prod"
  }
}
