{
  "name": "alpha-terminal",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "start": "pnpm --filter @alpha/api start:prod"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "turbo": "latest"
  }
}
